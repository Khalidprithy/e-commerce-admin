import PrimaryLayout from '@/components/layouts/PrimaryLayout';

const Shop = () => {
  return <div></div>;
};

Shop.getLayout = (page) => <PrimaryLayout title={'Shop'}>{page}</PrimaryLayout>;

export default Shop;
